part of 'values.dart';

abstract class AppPadding {
  static const recomendedSection = EdgeInsets.symmetric(horizontal: 16.0);
  static const recomendedSectionLowHeight = EdgeInsets.symmetric(horizontal: 16.0, vertical: 8);
  static const recomendedSectionHeader = EdgeInsets.symmetric(horizontal: 8.0, vertical: 16);

  static const recomendedCard = EdgeInsets.symmetric(horizontal: 4.0);
  static const recomendedCardText = EdgeInsets.symmetric(horizontal: 8.0);
  static const recomendedCardImageText = EdgeInsets.all(12);
}
